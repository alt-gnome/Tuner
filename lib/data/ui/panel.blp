using Gtk 4.0;
using Adw 1;

template $TunerPanel : Adw.NavigationPage {
    
    Adw.NavigationView main_nav_view {
        
        $TunerPanelPage main_page {
            title: bind template.title;
            tag: "main";
        }
    }
}
