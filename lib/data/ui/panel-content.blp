using Gtk 4.0;
using Adw 1;

template $TunerPanelContent : Adw.NavigationPage {

    Adw.ToolbarView toolbar_view {
        content: bind template.content;

        [top]
        Adw.HeaderBar header_bar {}
    }
}
