using Gtk 4.0;
using Adw 1;

template $TunerPanelPage : Adw.NavigationPage {
    
    Adw.NavigationView main_nav_view {
        
        $TunerPage main_page {
            title: bind template.title;
            tag: "main";
        }
    }
}
